# Redrob AI Candidate Ranker - Dependencies
# Core Python standard library is sufficient for the ranking pipeline.
# No external ML models or heavy dependencies needed.
# Python 3.9+ required.

# Optional: for Streamlit demo app
streamlit>=1.28.0

# For reading .docx files (optional, for extracting JD text)
python-docx>=1.0.0
